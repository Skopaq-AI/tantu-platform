apiVersion: v2
name: tantu
description: TANTU Platform — backend + frontend + Gateway + Rollout
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: qdrant
    version: "1.15.5"
    repository: https://qdrant.github.io/qdrant-helm
    condition: qdrant.enabled
